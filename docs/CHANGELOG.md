# CHANGELOG

面向成员/Agent 可见的变化。内部重构与测试调整不逐条记录。

## 2026-09-16（第三轮）— 剪映可编辑性通过人工验收

- **可编辑剪映草稿已由人验收通过**：对象是**真实生产批次 251-300 的 50 词草稿**，
  六步全部正常——改一个词、移音标、换一条配音、换背景、保存重开、导出约 5 秒并连续播放，**无异常**。
  环境范围：本机 + 剪映 **11.4.2.14459**（跨机、跨版本需另行复验）。
- 这一条之前只能由机器证明"结构与时间一致"，现在"人打开能改、改完能存能导"有人证了。
- 仍未验：音色/响度评价、非开发成员独立操作、目标核显轻薄本与跨机。

## 2026-09-16（第二轮）— 真实产出与可用性进展（仍未发布）

- **真实 TTS 首次打通**：内置试音探针三音色各出 1 条（3 次请求），
  随后 **3 词真实端到端**（9 次请求）与 **首批 v2 真实 50 词生产批次 201-250**（150 次请求）全部成功，
  累计消耗 162/20000 次。批次 201-250：真实合成 150 条音频、74.8s 出齐三产物，
  独立验收器按 v2 口径判定 **PASS**（172 文件、150 阶段计时 0 问题、逐词像素 151 探针 0 问题、
  音画 e2e 0 问题）。产物在 `out\production\201-250`。
- **朗读文本清洗（v2）成为默认**：中文朗读文本会去掉词性标记与其连接符、内嵌音标（保留显示释义原样）。
  本批 50 词中有 3 个词因此变化（#223/#234/#243），产物字幕与合成文本均已按 v2 落地。
- **成员免环境包**：`out\packages\word-video-member-*`（自带运行时与 FFmpeg，双击启动器即可运行），
  已在受限环境独立复验（PATH 仅系统目录、无 Python/FFmpeg/凭据）跑通 50 词并出齐三产物。
- **可编辑剪映草稿**：新副本已放进剪映草稿目录 `单词速记_P1三词待验_20260916`（结构与路径自检通过）。
- 旧链行为未改：同一请求在旧链与新入口下产出的五轨 SRT **逐字节相同**，归档复现 **timeline 0 差异**。
- **未验项照旧**：真实剪映里人工改稿/听感、目标核显轻薄本、跨机、签名与正式发布。

## 2026-09-16 — R1.1 接管基座（未发布候选）
- 建立 D 盘工作根与仓库基座：源码/测试/旧字幕工厂**复制**迁入，原件不动。
- 本机首次引入 Git 版本管理（此前无 `.git`）。
- 合并 R1.1 原则补丁：整体最优原则、现场路径与命令、角色边界、可踩检查清单、
  TTS 授权口径（20000 次请求、Q03 允许真实调用）。
- **P1 早期三词候选包**（本机可用，未发布、未签核）：真实媒体跑通三产物 =
  1080p60 带片头 MP4 + 五轨 SRT + 可编辑剪映草稿（含所需媒体），目录式候选包用硬链接发布，
  独立验收器八面 PASS、逐词像素 0 问题。**未验：真实剪映人工编辑、听感、目标核显机、跨机。**
- 无用户可见功能变化：尚无新软件入口，旧 CLI 行为未改。
