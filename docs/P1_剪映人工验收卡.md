# P1 剪映人工验收卡（唯一无法用代码替代的一步）

机器能证明的：草稿结构自检通过、17 个媒体/字体资源齐、9 条配音 + 9 条文字对象独立存在、
时间与 `timeline.json` 一致（独立验收器八面 PASS）。
机器**不能**证明的：**人打开剪映能不能真的改、能不能导出**。这一步只能由人做。

## 待验草稿

| 来源 | 路径 |
|---|---|
| M0 三词副本（已在剪映草稿目录，推荐先用这个） | `D:\jianying\JianyingPro Drafts\单词速记_M0三词待验_20260916` |
| P1 候选包内草稿（同一引擎、同三词，如需另存） | `D:\1\1-AI_workflow\word_video_flow\out\candidates\p1-三词候选包\batch\editable-draft` |

两处内容等价（同一引擎、同 151-153 三词）；**只做一次人工验收即可**，不需要各做一遍。

## 六步（约 5 分钟）

1. 打开剪映 → 打开草稿 `单词速记_M0三词待验_20260916`：确认**文字不是图片/不是烧录**，
   点一下能选中、能改字。
2. **改一个词**：把第一个英文单词改成任意临时文字（例如 `evaluateX`），看是否只影响这一段。
3. **移音标**：把某一条音标文字上下拖动，确认只有它动、英文/中文不动。
4. **换一条配音**：把某一条中文配音删掉或换成别的音频，确认时间线上其他对象不被带走。
5. **换背景**：替换背景视频/图片，确认文字与配音保留。
6. **保存 → 关闭 → 重开**：确认改动还在；然后**导出 5 秒**并连续播放，确认画面与声音正常
   （不该出现中文释义被念出词性，例如"n. 苹果"）。

## 需要回传的四件事

1. 每一步是**成功 / 失败 / 部分成功**（失败请写剪映的报错原文或截图）。
2. 剪映**实际版本号**（帮助 → 关于，或安装目录版本），以及是否提示过"草稿由更高版本创建"。
3. 第 6 步导出的文件路径与时长。
4. 任何"看起来能改、其实改了没用"的情况——这类比直接报错更危险，请务必写出来。

## 状态说明（不要混淆）

- 本卡完成前：P1 的"真剪映副本修改重开"一栏只能写 **待验**，不能写通过。
- 本卡完成后：把结果贴回对话，我会把它记进 `docs/STATUS.md`，并只在**这一台机器 + 这一个
  剪映版本**的范围内声明"剪映可编辑已验"（跨机、跨版本仍需分别复验）。
