[pytest]
testpaths = tests
norecursedirs = legacy_tests worktrees runtime data out logs .git
# 默认套件必须保持"开发随手可跑"（秒级到 2 分钟内）。需要只读归档真实媒体、
# 一次十几分钟的重验收用 marker 标出（tests/conftest.py 注册），显式运行：
#   python -m pytest -m acceptance_media tests
addopts = -q -m "not acceptance_media"
