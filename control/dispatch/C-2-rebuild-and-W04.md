# 派工单 C-2｜成员包重建（合并后）+ W04 连续预览启动

- **基础 commit**：合并后的 `main`（含 A 的 W01、B 的 W02；H0 会给准确 hash）
- **绝对 worktree**：`D:\1\1-AI_workflow\word_video_flow\worktrees\C`
  （先 `git fetch` 不需要；H0 会告诉你如何取新 main。建议 `git checkout -b task/C2 main`）
- **角色提示词**：`control/prompts/C.md`
- **前置**：你的 W09 早期包已完成（我尚未独立复验，只看了你的证据）

## 任务 1（先做，约 3 分钟）：把成员包重建到合并后的引擎并交 QA

你交付的包构建自 `1d00929`（合并 A/B **之前**）。main 现在已并入 W01（新工程模型）与
W02（媒体包/片头策略/缓存分离），所以：

1. `git checkout -b task/C2 main`（换基，不要在旧 `task/C` 上叠）
2. 用合并后的代码重建：`build_member_package.py --force`
3. 复验：`verify_member_package.py --package <新包> --report out\reports\w09-member-package-<ver>.verify.json`
4. 若复验出现**新**失败（合并引起的），先报告 H0 再动生产代码；不要为了打包通过改生产代码。

## 任务 2（主体）：W04 连续预览

目标：成员在**核显轻薄本**上编辑时不等整段重编码，拖动/定位后能连续播放且不混旧音。

要求（按 03 W04 与架构不变项）：
- 独立画布 + **统一 PCM 时钟**（音频主时钟，不是多播放器各播一轨凑同步）；
- 源素材**低清代理 / 按需解码**（B 已交 `word_video/media/proxy.py`，先读它，不要另写一套）；
- **generation seek**：seek 后旧世代的帧/音频必须被丢弃，不能出现旧音；
- **有界队列**与明确的过期状态；反复开关/定位要能释放资源（不许无限 join 线程卡 UI）；
- 本轮允许**先用测试布局适配器**验证时钟与队列（03 明确允许），
  但**完成时必须接 B 的 `LayoutSurface`**，不允许交两套排版实现；
  接口未冻结前先做时钟/队列/代理这三件与排版无关的事，并在报告里说明你等的接口是什么。
- 环境：PySide6 **6.11.2 已装在锁定 venv**（H0 实测 `QtWidgets`+`QtMultimedia` 可 import）。
  新增依赖必须先问 H0。
- 资源纪律：**GUI/长测试独占本机槽位**；一次只跑一个预览会话；
  演示用的媒体用真实 1080p 素材（`D:\单词速记自动化_测试归档_0915\_prepared-1080p`）与三词夹具，
  但要记录核显目标上的预算假设与实际测量（当前机器不是目标机，如实写"未在目标机验证"）。

## 可改范围（白名单）

`preview/**`、`desktop/**`、`packaging/**`、新增 `tests/test_preview_*.py`、`tests/test_desktop_*.py`

**禁止改**：`word_video/**`（A/B 的范围）、`word_video_cli.py`、根目录旧字幕工厂 6 文件、
`legacy_tests/**`、`AGENTS.md`、`control/**`、`docs/**`、其他 worktree、旧归档（只读）

## 验收（必须真跑并贴结果）

1. 预览启动到首帧的实测时间；一次 seek 到出画/出声的实测时间（给出数字与机器配置）。
2. 连续播放 **不混旧音**：用一段可辨识的音频（例如三词真实配音）做 seek 后立即播放的证据。
3. 音频重叠、静音区、循环背景三种情况正确。
4. 反复打开/关闭/定位 20 次后，进程树私有内存/句柄/线程/子进程的变化（不许只说"Python GC 正常"）。
5. 原生解码卡死有可恢复边界（写出你会怎么恢复，并给一个可复现的验证方式）。

## 禁止事项

只用绝对路径写文件；不新增依赖（先问 H0）；不 force push/不 merge；不递归派子 Agent；
不改生产代码让测试通过；不为了让预览"看起来流畅"悄悄降输出规格（预览可用低清代理，
但**成片规格不变**）。

## 报告格式（只报这四项）

改了什么（含 commit hash） → 测了什么及实际结果（含实测数字与资源数据） → 未验证什么 → 下一步/回滚 commit。
