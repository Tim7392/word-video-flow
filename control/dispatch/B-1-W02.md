# 派工单 B-1｜W02 旧视频最小修复与媒体链（第一波）

- **基础 commit**：`966c683e738c1c24becdb61b0bb4b0c343eb9af4`（分支 `task/B`）
- **绝对 worktree**：`D:\1\1-AI_workflow\word_video_flow\worktrees\B`
- **锁定解释器**：`D:\1\1-AI_workflow\word_video_flow\runtime\venv\Scripts\python.exe`
  自测时把 TEMP/TMP 指到 `D:\1\1-AI_workflow\word_video_flow\runtime\tmp\B`（自己建）。
- **角色提示词**：`control/prompts/B.md`（先读）

## 为团队解除什么阻塞

旧线已经有能出三产物的真实链路，但媒体层还有会**静默出错**的地方：
片头音轨策略、原始 TTS 与加工缓存混在一起、变速容易变成截尾或双变速、
音频重叠无法表达。这些不修，新软件的 W03（三产物）就会把旧问题继承下来，
成员会拿到"看着成功、其实听感错了"的成片。

## 可改范围（白名单）

- `word_video/{media,voices,layout,exporters}/**`（`layout`、`exporters` 可先建空包）
- **允许的旧线修复副本**：`word_video/render/**`（仅当修复确属片头/混音/变速策略，
  且必须附回归测试）；`word_video/timing.py` 仅在**变速不截尾**相关修复时最小改动
- 测试：`tests/test_wv_media_*.py`、`tests/test_wv_audio_*.py`（新增）；
  既有 `tests/test_word_video_*.py` 只能在**新增覆盖**时追加用例，不得删改原断言
- **禁止改**：`word_video/{domain,application,storage,cli}/**`（A 的范围）、
  根目录旧字幕工厂 6 文件与 `legacy_tests/**`、`word_video_cli.py` 的 CLI 契约（A 管）、
  `AGENTS.md`、`control/**`、`docs/**`、其他 worktree、仓库外任何路径（旧归档只读）

## 交付物

1. **片头音轨策略统一**：有音轨用音轨、无音轨走静音片头区（M0 已修 `render/mix.py` +
   `media.has_audio`，请**复核并补齐反例**），外置 `intro_audio` 与片头自带音轨
   不能重复发声；`ass=` 仍是滤镜链最后一级。
2. **原始 TTS 与加工缓存分离**：`original.*`（原始）与 `prepared.wav`（加工后）职责明确，
   改字号**不得触发合成**，改速度**只加工**、不重复调用 TTS。
3. **新媒体接口**（`word_video/media`）：有限音频重叠 + 音量、代理文件、实际 PTS/样本数读取；
   不把旧"非重叠"模型硬扩成新工程模型（新工程模型由 A 的 W01 提供）。
4. **测试（必须真跑并贴结果）**：
   - 正常缓存出片**不联网**（用 `runtime/venv` 跑；测试进程有离线守卫）；
   - 有片头 / 无片头 / 无声片头 + 外置音频四种组合不重复发声；
   - 变速不截尾、不双变速（逐样本/时长断言，不用"看起来差不多"）；
   - 改字号不合成、改速度只加工（用假 provider 记录调用次数=0）；
   - 源音频重叠在基础混音可表达，教学策略独立阻断；
   - 旧字幕与旧归档**未被修改**（跑完对 `D:\单词速记自动化_测试归档_0915` 只读校验，
     不要写任何文件到该目录）。

## 可复用夹具（只读，绝对路径）

- 三词真实请求（已指向 WORK_ROOT 输出）：`D:\1\1-AI_workflow\word_video_flow\data\fixtures\p1-有片头.json`、
  `p1-无片头.json`、`p1-无声片头.json`（每个 9 条真实缓存音频，本地 provider，无网络）
- 1080p 背景与透明片头：`D:\单词速记自动化_测试归档_0915\_prepared-1080p\`
- 真实缓存音频：见请求 JSON 里的 `provider.items[].path`
- M0 已验证的反例集与验收器：`D:\1\1-AI_workflow\word_video_flow\tools\m0\`（只读参考，
  QA 负责正式化；你**不要**改它）

## 禁止事项

**只用绝对路径写文件**（子 Agent 默认 cwd 是 `D:\1\1-AI_workflow`，不是你的 worktree；
探针实测绝对路径可写、无需额外授权）。禁止写 `D:\1\1-AI_workflow` 下的任何位置，
旧归档只读。不 force push、不 merge、不切主分支；不改共享 venv/依赖；不递归派子 Agent；
**重编码任务独占本机槽位**（同一时间只跑 1 个渲染/长测试）；不降输出规格、
不截尾、不双变速、不静默换音色/字体；不写 Python 逐像素循环。

## 报告格式（只报这四项）

改了什么（含 commit hash） → 测了什么及实际结果（命令 + 真实输出摘要） → 未验证什么 → 下一步/回滚 commit。
