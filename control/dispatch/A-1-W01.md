# 派工单 A-1｜W01 唯一工程与编辑命令（第一波）

- **基础 commit**：`966c683e738c1c24becdb61b0bb4b0c343eb9af4`（分支 `task/A`）
- **绝对 worktree**：`D:\1\1-AI_workflow\word_video_flow\worktrees\A`
- **锁定解释器**：`D:\1\1-AI_workflow\word_video_flow\runtime\venv\Scripts\python.exe`
  自测时把 TEMP/TMP 指到 `D:\1\1-AI_workflow\word_video_flow\runtime\tmp\A`（自己建）。
- **角色提示词**：`control/prompts/A.md`（先读）

## 为团队解除什么阻塞

现在没有任何"可编辑工程"的唯一真相：模板 JSON、请求 JSON、`timeline.json` 各说一套，
UI/CLI/导出迟早各写一套规则，成员和 Agent 都需要手改 JSON。
本任务先把**最小的、稳定的工程接口 + 一套编辑命令**立起来，让 W02（媒体）、
W03（三产物）、W05（存储/CLI）、W04（预览）都能挂到同一个业务核心上，
而不是各自复制一份时序/求解逻辑。

## 可改范围（白名单）

- 新增：`word_video/domain/**`、`word_video/application/**`、`word_video/storage/**`（可空最小实现）
- 新增测试：`tests/test_wv_project_*.py`、`tests/test_wv_commands_*.py`
- 允许改：`word_video/__init__.py`（仅加子包导出）
- **禁止改**：`word_video/{timing,tts,media,draft,template,voices,srt_export,render/**}`（B 的范围）、
  根目录旧字幕工厂 6 文件（`subtitle_factory_*.py`、`Words_SRT.py`、`word.py`）与
  `legacy_tests/**`、`tests/test_word_video_*.py`（既有回归）、`AGENTS.md`、`control/**`、`docs/**`、
  其他 worktree、仓库外任何路径。

## 交付物

1. **最小接口一版（接口版本 `wv-project@1`）**，放 `word_video/domain/`：
   `Project`、`Clip`、`TimeExpr`、`MediaSlice`、`RenderPlan`、`CuePlan`。
   要求：`TimeExpr` 以 **720000 tick/s** 为整型基准并带**有理数源时基**换算；
   `Project` 是唯一可编辑真相；`RenderPlan`/`CuePlan` 是**只读派生**（不能反向写工程）。
   字段只保留当前消费者真正需要的；不做通用 schema 平台。
2. **固定手算夹具**（`tests/test_wv_project_golden.py`）：5.5 秒算例，
   期望值由**手算写死在测试里**，不得用生产 `solve()` 生成期望。
3. **编辑命令**（`word_video/application/`）：单片段移动/修剪、显式整组联动、
   撤销/重做、保存/重开、模板实例化（仅在创建/显式升级时展开）。
   同一套命令将来给 CLI 和 UI 共用，命令必须是纯函数式输入→结果+新状态，不依赖 Qt。
4. **求解与校验**：教学角色顺序（英文女声→英文男声→中文释义）、重叠策略
   （教学交付重叠必须拒绝；允许表达基础混音重叠但不悄悄放过）。
5. **测试**（必须真跑并贴结果）：
   - 5.5 秒算例通过；
   - 男声移动 0.2 秒，其他对象 tick 不变（逐对象断言，不用聚合值）；
   - 教学重叠被拒绝（含最小反例）；
   - 撤销恢复依赖链；
   - 重复 ID / 环 / 悬空引用 / 未知时长 / 过期 revision 全部失败；
   - 模板/高级模式切换不改业务数据；
   - 30/60/非整数帧率与 44.1kHz 源有正确换算（给出具体数值断言）。

## 复用要求（不要推倒重写）

现有 `word_video/timing.py` 的节奏语义与常量（1.25 默认速度、英文下限 1.0、中文下限
`max(0.5, min(n,6)*0.4+(n-6)*0.2)`、gap 0.1）已被 M0 独立复算验证过：**复用其行为**，
不要在 domain 里另立一套节奏规则；需要时只读引用，不改该文件。

## 禁止事项

**只用绝对路径写文件**（子 Agent 默认 cwd 是 `D:\1\1-AI_workflow`，不是你的 worktree；
探针实测绝对路径可写、无需额外授权）。禁止写 `D:\1\1-AI_workflow` 下的任何位置。
不 force push、不 merge、不切主分支；不改共享 venv/依赖；不递归派子 Agent；不改受保护路径；
不降低验收阈值、不删断言、不用 SKIP 顶替；不静默改默认行为；不写 Python 逐像素/逐样本循环。

## 报告格式（只报这四项）

改了什么（含 commit hash） → 测了什么及实际结果（命令 + 真实输出摘要） → 未验证什么 → 下一步/回滚 commit。
