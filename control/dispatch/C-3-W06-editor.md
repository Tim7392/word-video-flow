# 派工单 C-3｜W06 成员可用的编辑器（主体；CLI 同步部分等 W05）

- **基础 commit**：合并后的 main（含 W01/W02/W03/W04/A-4 `wv-project@2`；H0 会给 hash）
- **绝对 worktree**：`D:\1\1-AI_workflow\word_video_flow\worktrees\C`（`git checkout -b task/C3 main`）
- **角色提示词**：`control/prompts/C.md`
- **前置**：W04 连续预览（你刚交）、W03 `LayoutSurface`（已消费）、A 的 `wv-project@2`
- **不在本轮**：CLI 与 UI 的"同一 revision 同步"（依赖 A 的 W05 协调器/收据）；等 W05 合并后我再派

## 为团队解除什么阻塞

现在成员要改一个词的时间、换个字号、剪掉一段，只能手改 JSON 或用旧工具。
W04 已经把"看得见"做出来了（时钟/代理/seek 都实测达标），但**还没有"改得动"**：
没有画布上的选择、拖动、修剪、拆分、撤销与保存重开。这一步做完，成员才算真的能用它出片，
而不是让 Agent 代劳。

## 可改范围（白名单）

- `desktop/**`（编辑器 UI、选择/拖动/修剪/拆分、属性面板、错误定位面板）
- `preview/**`（仅为编辑器需要的最小扩展：例如选中态/吸附辅助线所需的查询接口）
- 新增：`tests/test_desktop_editor_*.py`、`tests/test_editor_*.py`
- **禁止改**：`word_video/**`（A/B 的范围；需要接口就报 H0 转达）、`packaging/**`（W09 已交付，别动）、
  旧字幕核心、`AGENTS.md`、`control/**`、`docs/**`、其他 worktree、旧归档

## 要求

1. **默认点选单片段**；单片段移动**不默默带动**未选对象。整组/联动必须是**显式操作**
   （A 的 `MoveClips`/`BindStart`），且跟随者要在提示里列出。
2. **独立拖动/修剪/拆分**：全部经 A 的 application 命令（`apply/session`），
   **不要在 UI 里自己改 tick**；修剪后留空隙不报错，违反教学顺序/重叠时按 A 的错误码给出**可操作提示**
   （定位到词/角色 + 修复入口）。
3. **撤销/重做/保存/重开**：用 A 的 `Session` 与 `project_store`；重开后编辑仍在；
   多轮模板/高级模式切换不丢编辑（模式不改业务数据）。
4. **输入预检与可点修复**：缺媒体/缺字体/缺音色/工程过期要有可点修复动作，
   不要弹一个只有"确定"的对话框，也不要把成员丢进 JSON。
5. **常用窗口/缩放/DPI 可操作**：给最小但真实的窗口（画布 + 时间线 + 属性面板 + 错误面板），
   不追求玻璃装饰；**不要用逐像素 UI 截图当业务测试**。
6. **预览与成片一致**：画布绘制必须用 `LayoutSurface` 的 placements（你在 W04 已验证三画布逐位一致），
   不允许为编辑器另写排版。
7. **资源纪律**：一次只跑一个预览会话；编辑不等整段重编码（W04 已做到，别破坏它）。

## 验收（必须真跑并贴结果）

1. **三词从导入到三产物**：用 UI 或 UI 驱动的命令走完整流程（可导出后交独立验收器判 PASS），
   过程中**不手改 JSON**。
2. 单片段拖动：只动选中的那个（逐对象断言 tick 不变）；显式整组移动：跟随者动了且被列出。
3. 修剪/拆分后：撤销能回到原状（逐对象断言）；教学重叠/顺序错误给出**定位到词与角色**的提示。
4. 保存→关闭→重开→再编辑→再保存：无丢改动、无残留临时文件。
5. 模式切换 3 次以上：业务数据（`to_dict()`）逐字不变。
6. DPI/窗口：至少两个缩放档位下可操作（记录你实际用的取值）。

## 禁止事项

只用绝对路径写文件；不新增依赖（先问 H0）；不改生产代码让 UI 好写；不 force push、不 merge；
不递归派子 Agent；**GUI 测试独占本机槽位**；不为了让界面流畅悄悄降输出规格。

## 报告格式（只报这四项）

改了什么（含 commit hash） → 测了什么及实际结果（命令/操作步骤 + 真实数字与判定） →
未验证什么（例如未在目标核显机、未做人工可用性测试） → 下一步/回滚 commit。
